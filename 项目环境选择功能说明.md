# 真实API集成 - 项目和环境选择

## ✅ 已完成的修改

### 使用真实API替换模拟数据

现在项目和环境选择器使用真实的后端API，而不是硬编码的模拟数据。

## API集成详情

### 1. 获取项目列表

**API端点**: `GET /api/projects/`

**请求**:
```http
GET http://114.66.56.200:6002/api/projects/
Authorization: Bearer {access_token}
```

**响应**:
```json
[
  {
    "id": "project_123",
    "name": "销售管理系统",
    "description": "项目描述",
    "project_manager_id": "user_456",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59"
  },
  ...
]
```

### 2. 获取环境列表

**API端点**: `GET /api/environments/project/{project_id}`

**请求**:
```http
GET http://114.66.56.200:6002/api/environments/project/project_123
Authorization: Bearer {access_token}
```

**响应**:
```json
[
  {
    "id": "env_001",
    "name": "开发环境",
    "url": "http://dev.example.com",
    "username": "admin",
    "description": "开发环境配置"
  },
  {
    "id": "env_002",
    "name": "测试环境",
    "url": "http://test.example.com",
    "username": "admin",
    "description": "测试环境配置"
  },
  ...
]
```

## 工作流程

```
用户登录
  ↓
调用 GET /api/projects/
  ↓
获取用户参与的所有项目
  ↓
显示在项目下拉框中
  ↓
用户选择项目
  ↓
调用 GET /api/environments/project/{project_id}
  ↓
获取该项目的所有环境
  ↓
显示在环境下拉框中
  ↓
用户选择环境
  ↓
保存当前选择
```

## 代码实现

### 数据类定义

```kotlin
private data class ProjectItem(
    val id: String,
    val name: String,
    val description: String?,
    val project_manager_id: String?,
    val start_date: String?,
    val end_date: String?
)

private data class EnvironmentItem(
    val id: String,
    val name: String,
    val url: String,
    val username: String?,
    val description: String?
)
```

### 加载项目列表

```kotlin
private fun loadProjectsAndEnvironments() {
    ApplicationManager.getApplication().executeOnPooledThread {
        try {
            // 调用API获取项目列表
            val projectsResponse = HttpClient.get(
                endpoint = ApiConfig.Projects.LIST,
                requireAuth = true,
                responseType = Array<ProjectItem>::class.java
            )
            
            // 更新UI
            ApplicationManager.getApplication().invokeLater {
                projectComboBox.removeAllItems()
                projectNameToId.clear()
                
                projectComboBox.addItem("-- 请选择项目 --")
                
                if (projectsResponse.success && projectsResponse.data != null) {
                    projectsResponse.data.forEach { project ->
                        projectComboBox.addItem(project.name)
                        projectNameToId[project.name] = project.id
                    }
                    
                    // 加载第一个项目的环境
                    if (projectsResponse.data.isNotEmpty()) {
                        loadEnvironmentsForProject(projectsResponse.data[0].id)
                    }
                }
            }
        } catch (e: Exception) {
            println("加载项目列表失败: ${e.message}")
        }
    }
}
```

### 加载环境列表

```kotlin
private fun loadEnvironmentsForProject(projectId: String) {
    ApplicationManager.getApplication().executeOnPooledThread {
        try {
            // 调用API获取环境列表
            val endpoint = "/api/environments/project/$projectId"
            val environmentsResponse = HttpClient.get(
                endpoint = endpoint,
                requireAuth = true,
                responseType = Array<EnvironmentItem>::class.java
            )
            
            // 更新UI
            ApplicationManager.getApplication().invokeLater {
                environmentComboBox.removeAllItems()
                environmentComboBox.addItem("-- 请选择环境 --")
                
                if (environmentsResponse.success && environmentsResponse.data != null) {
                    environmentsResponse.data.forEach { env ->
                        environmentComboBox.addItem(env.name)
                    }
                }
            }
        } catch (e: Exception) {
            println("加载环境列表失败: ${e.message}")
        }
    }
}
```

### 项目选择处理

```kotlin
private fun handleProjectChange() {
    val selectedProject = projectComboBox.selectedItem as? String
    if (selectedProject != null && !selectedProject.startsWith("--")) {
        currentProject = selectedProject
        
        // 获取项目ID并加载对应的环境
        val projectId = projectNameToId[selectedProject]
        if (projectId != null) {
            loadEnvironmentsForProject(projectId)
        }
        
        // 显示通知
        addNotification(
            "项目切换",
            "已切换到: $currentProject",
            NotificationType.INFO
        )
    }
}
```

## 特性

### 1. 动态加载

- ✅ 登录成功后自动从API获取项目列表
- ✅ 选择项目后自动加载该项目的环境列表
- ✅ 所有数据来自后端，无硬编码

### 2. 错误处理

- ✅ API调用失败时显示"加载失败"
- ✅ 在控制台输出详细错误信息
- ✅ 不会阻塞UI线程

### 3. 用户体验

- ✅ 后台异步加载，不阻塞UI
- ✅ 加载完成后自动更新下拉框
- ✅ 切换项目时显示通知
- ✅ 切换环境时显示通知

### 4. 数据管理

- ✅ 使用Map存储项目名称到ID的映射
- ✅ 支持根据项目ID获取环境
- ✅ 自动清理旧数据

## 使用示例

### 登录后自动加载

```
用户登录成功
  ↓
自动调用 loadProjectsAndEnvironments()
  ↓
显示项目列表:
  - 销售管理系统
  - 客户关系管理
  - 库存管理系统
  ↓
自动加载第一个项目的环境
  ↓
显示环境列表:
  - 开发环境
  - 测试环境
  - 生产环境
```

### 切换项目

```
用户选择 "客户关系管理"
  ↓
触发 handleProjectChange()
  ↓
获取项目ID: "project_456"
  ↓
调用 loadEnvironmentsForProject("project_456")
  ↓
更新环境列表:
  - UAT环境
  - 生产环境
  ↓
显示通知: "已切换到: 客户关系管理"
```

## 调试信息

### 控制台输出

```
API Request: GET http://114.66.56.200:6002/api/projects/
API Response [200]: [{"id":"project_123","name":"销售管理系统",...}]
切换到项目: 销售管理系统
API Request: GET http://114.66.56.200:6002/api/environments/project/project_123
API Response [200]: [{"id":"env_001","name":"开发环境",...}]
```

### 错误情况

```
API Request: GET http://114.66.56.200:6002/api/projects/
API Response [401]: {"detail":"Unauthorized"}
获取项目列表失败: 请求失败: HTTP 401
```

## 对比：修改前 vs 修改后

### 修改前（模拟数据）

```kotlin
val projects = listOf(
    "项目A - 销售管理系统",
    "项目B - 客户关系管理",
    "项目C - 库存管理系统",
    "项目D - 财务管理系统"
)

val environments = listOf(
    "开发环境 (DEV)",
    "测试环境 (TEST)",
    "预发布环境 (UAT)",
    "生产环境 (PROD)"
)
```

**问题**:
- ❌ 硬编码的数据
- ❌ 无法反映真实项目
- ❌ 环境与项目无关联
- ❌ 无法动态更新

### 修改后（真实API）

```kotlin
val projectsResponse = HttpClient.get(
    endpoint = ApiConfig.Projects.LIST,
    requireAuth = true,
    responseType = Array<ProjectItem>::class.java
)

val environmentsResponse = HttpClient.get(
    endpoint = "/api/environments/project/$projectId",
    requireAuth = true,
    responseType = Array<EnvironmentItem>::class.java
)
```

**优势**:
- ✅ 真实的后端数据
- ✅ 反映用户实际参与的项目
- ✅ 环境与项目正确关联
- ✅ 支持动态更新

## 后续优化

### 1. 缓存机制

```kotlin
private val projectsCache = mutableMapOf<String, List<ProjectItem>>()
private val environmentsCache = mutableMapOf<String, List<EnvironmentItem>>()

fun loadProjects() {
    if (projectsCache.isEmpty()) {
        // 从API加载
    } else {
        // 使用缓存
    }
}
```

### 2. 刷新按钮

```kotlin
val refreshButton = JButton("刷新项目")
refreshButton.addActionListener {
    projectsCache.clear()
    loadProjectsAndEnvironments()
}
```

### 3. 搜索功能

```kotlin
val searchField = JTextField()
searchField.addKeyListener(object : KeyAdapter() {
    override fun keyReleased(e: KeyEvent) {
        filterProjects(searchField.text)
    }
})
```

## 常见问题

### Q: 为什么项目列表是空的？

A: 可能的原因：
1. 用户没有参与任何项目
2. API调用失败（检查控制台）
3. Token过期（需要重新登录）

### Q: 切换项目后环境列表没有更新？

A: 检查：
1. 控制台是否有错误信息
2. 项目ID映射是否正确
3. API是否返回了环境数据

### Q: 如何添加项目搜索功能？

A: 参考"后续优化"部分的搜索功能实现。

---

**更新时间**: 2025-12-24  
**版本**: v1.6.0
